apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sCommonPolicy
metadata:
  name: enforce-common-policy
spec:
  enforcementAction: deny
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]